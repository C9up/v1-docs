# Archive — File Storage

Statut: **Manquant dans `ream-dev`** en tant que package dedie.

- Cible package: `@c9up/archive`
- Etat actuel: stockage present dans `@c9up/ream` (drivers), pas de package separé.

Prochaine etape recommandee:

1. package storage autonome
2. drivers local/S3/R2
3. URL signing + policies d'acces
